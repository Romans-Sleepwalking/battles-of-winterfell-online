package DatabaseTests

import org.scalatest._

class Temporal_CSV_DB_Test extends FunSuite {

  test("Test CSV Database") {

    val DB: Temporal_CSV_Database = new Temporal_CSV_Database()

    assert(1 === 1)
  }
}